# 🎮 CONTROLLERS - Événements

**Priorité** : 🟡 MOYENNE | **Phase** : 4 | **Semaine** : 6+

---

## 📋 Description

Controllers pour événements, routes, participations et candidatures organisateur.

---

## ✅ Controller 1 : EventsController (NOUVEAU)

**Fichier** : `app/controllers/admin_panel/events_controller.rb`

**Code à implémenter** :
```ruby
module AdminPanel
  class EventsController < BaseController
    # À créer
  end
end
```

---

## ✅ Controller 2 : RoutesController (NOUVEAU)

**Fichier** : `app/controllers/admin_panel/routes_controller.rb`

**Code à implémenter** :
```ruby
module AdminPanel
  class RoutesController < BaseController
    # À créer
  end
end
```

---

## ✅ Controller 3 : AttendancesController (NOUVEAU)

**Fichier** : `app/controllers/admin_panel/attendances_controller.rb`

**Code à implémenter** :
```ruby
module AdminPanel
  class AttendancesController < BaseController
    # À créer
  end
end
```

---

## ✅ Controller 4 : OrganizerApplicationsController (NOUVEAU)

**Fichier** : `app/controllers/admin_panel/organizer_applications_controller.rb`

**Code à implémenter** :
```ruby
module AdminPanel
  class OrganizerApplicationsController < BaseController
    # À créer
  end
end
```

---

## ✅ Checklist Globale

### **Phase 4 (Semaine 6+)**
- [ ] Créer EventsController
- [ ] Créer RoutesController
- [ ] Créer AttendancesController
- [ ] Créer OrganizerApplicationsController
- [ ] Tester toutes les actions

---

**Retour** : [README Événements](./README.md) | [INDEX principal](../INDEX.md)
